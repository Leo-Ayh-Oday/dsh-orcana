# dsh-orcana Linux edition bundle patch. Inserts the hardening row with
# NEUTRAL defaults: installing the profile must not change DSH's execution
# semantics. Enforce layers by configuring the row (e.g. `network: none` or
# `resourceLimits`) or via a later --patch overlay keyed by row id.
- insert:
    - id: dsh-orcana-linux
      name: '@leooday/dsh-orcana-linux'
      config: {}
